<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.qf.mediaplayer_service">

    <application
        android:allowBackup="true"
        android:icon="@drawable/a59"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.qf.mediaplayer_service.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="com.example.qf.mediaplayer_service.MyMusicService"
            android:enabled="true"
            android:exported="true"></service>
        <!--<receiver android:name=".MainActivity$MyReceiver">-->
        <!--<intent-filter>-->
        <!--<action android:name="updateSeekBar"/>-->
        <!--</intent-filter>-->
        <!--</receiver>-->
    </application>

</manifest>